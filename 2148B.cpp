#include<iostream>
using namespace std;

int main(){
    int t = 1;
    cin >> t;
    while(t--){
        int n, m, x, y;
        cin >> n >> m >> x >> y;
        for(int  i = 0; i < n; i++){
            int temp;
            cin >> temp;
        }

        
        for(int  i = 0; i < m; i++){
            int temp;
            cin >> temp;
        }

        cout << n + m << '\n';
    }
}